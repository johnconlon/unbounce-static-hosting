import { h, Component } from "preact";
import { UnbounceForm } from "./form";

export interface Props {
  form: UnbounceForm;
  children: Function;
}

export default class UnbounceAdapter extends Component<Props> {
  onSubmit() {
    console.log("UNBOUNCE SUBMIT");
  }

  render(props: Props) {
    return props.children(this.onSubmit);
  }
}
