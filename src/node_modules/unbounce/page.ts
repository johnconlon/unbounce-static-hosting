import { getForm } from "./form";

export function init(cb: Function) {
  lp.jQuery(document).ready(() => {
    const container = document.body;
    const form = getForm();
    cb(container, form);
  });
}
